apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: rag-platform
  labels:
    app: vllm
spec:
  type: ClusterIP
  selector:
    app: vllm
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
