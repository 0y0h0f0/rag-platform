apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lancedb-pvc
  namespace: rag-platform
  labels:
    app: rag-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
